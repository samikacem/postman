{
  "name": "postman",
  "version": "1.0.0",
  "description": "postman test jenkins",
  "scripts": {
    "api-tests": "newman run Sami.postman_collection.json -e Sami.postman_environment.json -r junit,htmlextra --reporter-htmlextra-export results/result.html --reporter-junit-export results/result.xml"
  },
  "author": "sami kacem",
  "dependencies": {
    "newman": "^5.2.0",
    "newman-reporter-htmlextra": "1.19.2"
  }
}
