{
  "name": "postman",
  "version": "1.0.0",
  "description": "postman test jenkins",
  "scripts": {
    "api-tests": 'newman run Sami.postman_collection.json -e Sami.postman_environment.json --reporters cli,htmlextra --reporter-htmlextra-export "result.html"'
  },
  "author": "sami kacem",
  "dependencies": {
    "newman": "^5.2.0"
  }
}
